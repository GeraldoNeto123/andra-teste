import { styled } from "@mui/material";

export const FooterContainer = styled("footer")`
  position: absolute;
  bottom: 0;
  width: 100%;
  text-align: center;
  p {
    position: relative;
    z-index: 1;
  }
`;
